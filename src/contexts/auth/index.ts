
// Export both auth systems for compatibility
export { AuthProvider, useAuth } from './AuthProvider';
export { SupabaseAuthProvider, useSupabaseAuth } from './SupabaseAuthProvider';
export type { AuthContextType } from './types';
